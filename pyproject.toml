[project]
name = "kidney-exchange-guest-lecture"
version = "0.1.0"
description = "Kidney exchange guest lecture slides and supporting materials"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.0",
]
